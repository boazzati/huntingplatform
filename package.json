{
  "name": "afh-hunting-engine",
  "version": "1.0.0",
  "description": "AFH Hunting Engine - Full Stack Application",
  "private": true,
  "scripts": {
    "start": "cd backend && npm start",
    "dev": "cd backend && npm run dev",
    "build": "cd backend && npm install && cd ../frontend && npm install && npm run build"
  },
  "workspaces": [
    "backend",
    "frontend"
  ]
}
